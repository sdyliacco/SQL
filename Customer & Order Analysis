--This analysis utilizes a database with multiple tables containing customer and order data:

--How many orders were placed in January?
SELECT COUNT(DISTINCT orderid) 
FROM JanSales
WHERE length(orderID) = 6
AND orderID <> 'Order ID' ;

--How many of those orders were for an iPhone? 
SELECT COUNT(DISTINCT orderid) 
FROM JanSales
WHERE length(orderID) = 6
AND orderID <> 'Order ID' 
AND Product = 'iPhone' ;

--Select the customer account numbers for all the orders that were placed in February. 
SELECT DISTINCT acctnum 
FROM customers
JOIN FebSales
ON customers.order_id = FebSales.orderID 
WHERE length(orderID) = 6
AND orderID <> 'Order ID' ;

--Which product was the cheapest one sold in January, and what was the price? 
SELECT DISTINCT Product , price
FROM JanSales
WHERE price = (SELECT MIN(price) FROM JanSales) ;

--What is the total revenue for each product sold in January?
SELECT product , (SUM(quantity) * price) AS revenue
FROM JanSales
WHERE length(orderID) = 6
AND orderID <> 'Order ID'
GROUP BY product ;

--Which products were sold in February at 548 Lincoln St, Seattle, WA 98101, how many of each were sold, and what was the total revenue?
SELECT product , SUM(quantity) , (SUM(quantity)*price) AS revenue
FROM FebSales
WHERE location = '548 Lincoln St, Seattle, WA 98101' 
GROUP BY product ;

--How many customers ordered more than 2 products at a time, and what was the average amount spent for those customers? 
SELECT COUNT(DISTINCT acctnum) AS customers , ROUND(AVG(quantity*price),2) AS avg_spent
FROM FebSales
LEFT JOIN customers
ON customers.order_id = FebSales.orderID
WHERE length(orderID) = 6
AND orderID <> 'Order ID'
AND FebSales.quantity > 2 ;

--Find the location for the order placed on 02/18/19 at 1:35am.
SELECT location
FROM BIT_DB.FebSales
WHERE orderdate = '02/18/19 01:35' ;

--Find the number of products purchased on 02/18/19.
SELECT sum(quantity)
FROM BIT_DB.FebSales
WHERE orderdate like '02/18/19%' ;

--Find the order ID starting with 16, followed by one unknown character, followed by 0, and followed by any number of characters after.
SELECT orderID
FROM BIT_DB.MarSales
WHERE orderID LIKE '16_0%'

--How many of each type of headphone were sold in February?
SELECT SUM(quantity) , Product
FROM FebSales
WHERE Product LIKE '%headphone%'
AND length(orderid) = 6
AND orderid <> 'Order ID'
GROUP BY Product ;

--What was the average amount spent per account in February?
SELECT (SUM(quantity*price) / COUNT(acctnum))
FROM FebSales
JOIN customers
ON FebSales.orderid = customers.order_id 
WHERE length(orderid) = 6
AND orderid <> 'Order ID';

--Find order ids from Georgia (GA) where the order has an average price greater than $1,000. Only find orders from dates beginning with ‘04/1’.
SELECT orderid
FROM AprSales
WHERE location LIKE '%GA%'
AND orderdate LIKE '04/1%'
AND length(orderid) = 6
AND orderid <> 'Order ID'
GROUP BY orderid
HAVING AVG(price) > 1000 ;

--Find the location and order ID for IDs beginning with 1943 and ending with 5.
SELECT location , orderid
FROM MaySales
WHERE orderid LIKE '1943_5'
AND length(orderid) = 6
AND orderid <> 'Order ID' ;

--Which product brought in the most revenue in January and how much revenue did it bring in total?
SELECT product , SUM(quantity*price) AS revenue
FROM JanSales
WHERE length(orderid) = 6
AND orderid <> 'Order ID' 
GROUP BY product 
ORDER BY revenue desc
LIMIT 1 ;


--This portion of the analysis is visualized in my Tableau dashboard 'Tech Sales Report':

--Which products are sold?
SELECT DISTINCT product
FROM JanSales
WHERE length(orderid) = 6
AND orderid <> 'Order ID' ;

--Which product made the most revenue? Which month had the most revenue from that product?
SELECT product , SUM(price*quantity) as revenue
FROM JanSales
WHERE length(orderid) = 6
AND orderid <> 'Order ID' 
GROUP BY product
ORDER BY revenue desc
LIMIT 1 ;

SELECT product , SUM(price*quantity) as revenue
FROM FebSales
WHERE length(orderid) = 6
AND orderid <> 'Order ID' 
GROUP BY product
ORDER BY revenue desc
LIMIT 1 ;

SELECT product , SUM(price*quantity) as revenue
FROM MarSales
WHERE length(orderid) = 6
AND orderid <> 'Order ID' 
GROUP BY product
ORDER BY revenue desc
LIMIT 1 ;

SELECT product , SUM(price*quantity) as revenue
FROM AprSales
WHERE length(orderid) = 6
AND orderid <> 'Order ID' 
GROUP BY product
ORDER BY revenue desc
LIMIT 1 ;

SELECT product , SUM(price*quantity) as revenue
FROM MaySales
WHERE length(orderid) = 6
AND orderid <> 'Order ID' 
GROUP BY product
ORDER BY revenue desc
LIMIT 1 ;

--Which product had the highest quantity sold? Which month had the highest quantity?
SELECT product, SUM(quantity) as total_quantity
FROM JanSales
WHERE length(orderid) = 6
AND orderid <> 'Order ID' 
GROUP BY product
ORDER BY total_quantity desc
LIMIT 1 ;

SELECT product, SUM(quantity) as total_quantity
FROM FebSales
WHERE length(orderid) = 6
AND orderid <> 'Order ID' 
GROUP BY product
ORDER BY total_quantity desc
LIMIT 1 ;

SELECT product, SUM(quantity) as total_quantity
FROM MarSales
WHERE length(orderid) = 6
AND orderid <> 'Order ID' 
GROUP BY product
ORDER BY total_quantity desc
LIMIT 1 ;

SELECT product, SUM(quantity) as total_quantity
FROM AprSales
WHERE length(orderid) = 6
AND orderid <> 'Order ID' 
GROUP BY product
ORDER BY total_quantity desc
LIMIT 1 ;

SELECT product, SUM(quantity) as total_quantity
FROM MaySales
WHERE length(orderid) = 6
AND orderid <> 'Order ID' 
GROUP BY product
ORDER BY total_quantity desc
LIMIT 1 ;

--Which location has highest revenue? (Different limitations to view all tied results)
SELECT location, SUM(quantity*price) as revenue
FROM JanSales
WHERE length(orderid) = 6
AND orderid <> 'Order ID' 
GROUP BY location
ORDER BY revenue desc
LIMIT 1 ;

SELECT location, SUM(quantity*price) as revenue
FROM FebSales
WHERE length(orderid) = 6
AND orderid <> 'Order ID' 
GROUP BY location
ORDER BY revenue desc
LIMIT 3 ;

SELECT location, SUM(quantity*price) as revenue
FROM MarSales
WHERE length(orderid) = 6
AND orderid <> 'Order ID' 
GROUP BY location
ORDER BY revenue desc
LIMIT 1 ;

SELECT location, SUM(quantity*price) as revenue
FROM AprSales
WHERE length(orderid) = 6
AND orderid <> 'Order ID' 
GROUP BY location
ORDER BY revenue desc
LIMIT 1 ;

SELECT location, SUM(quantity*price) as revenue
FROM MaySales
WHERE length(orderid) = 6
AND orderid <> 'Order ID' 
GROUP BY location
ORDER BY revenue desc
LIMIT 1 ;

--Which address has the most orders? (Different limitations to view all tied results)
SELECT location, COUNT(orderID) as total_orders
FROM JanSales
WHERE length(orderid) = 6
AND orderid <> 'Order ID' 
GROUP BY location
ORDER BY total_orders desc
LIMIT 4 ;

SELECT location, COUNT(orderID) as total_orders
FROM FebSales
WHERE length(orderid) = 6
AND orderid <> 'Order ID' 
GROUP BY location
ORDER BY total_orders desc
LIMIT 1 ;

SELECT location, COUNT(orderID) as total_orders
FROM MarSales
WHERE length(orderid) = 6
AND orderid <> 'Order ID' 
GROUP BY location
ORDER BY total_orders desc
LIMIT 3 ;

SELECT location, COUNT(orderID) as total_orders
FROM AprSales
WHERE length(orderid) = 6
AND orderid <> 'Order ID' 
GROUP BY location
ORDER BY total_orders desc
LIMIT 2 ;

SELECT location, COUNT(orderID) as total_orders
FROM MaySales
WHERE length(orderid) = 6
AND orderid <> 'Order ID' 
GROUP BY location
ORDER BY total_orders desc
LIMIT 5 ;
